---
# Should not be changed
project_uid: "company/project"
dashboard_uid: "dashboard"
dashboard:
    home_dir: "/opt/{{ project_uid }}/{{ dashboard_uid }}"
    log_dir: "/var/log/{{ project_uid }}/{{ dashboard_uid }}"
